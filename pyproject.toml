[project]
name = "algorithm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "GaoZhiHong", email = "gaozhihong73@163.com" }]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
algorithm = "algorithm:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
